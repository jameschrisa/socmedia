node_modules
dist
*.log
.env
# runtime database + user uploads (any workspace)
data/
**/data/
test-results
playwright-report
.DS_Store
.vercel
# operator credentials (see ops/README.md) — never committed, never pasted into a chat
ops/.env.ops
